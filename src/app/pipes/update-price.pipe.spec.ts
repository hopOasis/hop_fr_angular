import { UpdatePricePipe } from './update-price.pipe';

describe('UpdatePricePipe', () => {
  it('create an instance', () => {
    const pipe = new UpdatePricePipe();
    expect(pipe).toBeTruthy();
  });
});
